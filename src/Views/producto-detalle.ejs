<!DOCTYPE html>
<html lang="es">
  <head>
    <%- include('./partials/head.ejs')  %>
    <link rel="stylesheet" href="/css/detalleproducto.css"/>
</head>
<body>

  <%- include('./partials/header.ejs')  %>

<main>
  <%if(data != null && data.subcategorias != null & data.subcategorias.length > 0) {%>
    <div class="submenu-productos">
      <nav class="nav-full-screen">
        <div>
          <a href="/productos/<%= data.producto[0].categoria_id%>">TODO</a>
        </div>
        <% data.subcategorias.forEach(function(subcategoria){%>
          <div>
            <a href="/productos/<%= subcategoria.categoria_id%>/<%= subcategoria.nombre%>">
              <%= subcategoria.nombre%>
            </a>
          </div>
        %>
        <% }) %>
      </nav>
    </div>
    <%}%>

  <div class="main-content">
    <nav class="menu-detalle-producto" style="justify-content: center;">
      <p class="producto-seleccionado"><a href="/">GOLF DE ALTURA STORE - PRODUCTO SELECCIONADO <%=data.producto[0].p_nombre%></a></p> 
    </nav>
    <div class="product-content">
      <div class="col1 img-primaria">
        <img src="/images/productos/<%=data.producto[0].imagen %>" class="img1"width="80" height="60" alt="">
      </div>
      <div class="col2">
        <div class="product-name">
          <% let finalPrice = (data.producto[0].precio * ((100 - data.producto[0].descuento) / 100)).toFixed(0)%>
          <% if(data.producto[0].descuento === "0.00"){ %>
            <h5><span class="span-2">  $ <%= toThousand(data.producto[0].precio) %> </span> </h5>
          <% } else{ %>  
            <h5><span class="span-1">$ <%= toThousand(data.producto[0].precio) %> </span> <span class="span-2">$ <%= toThousand(finalPrice) %> </span></h5>
            <p class="p-descuento">Ahorra <%= data.producto[0].descuento %> % </p>
          <% } %>
        </div>  
          
        <% if(data.producto[0].marca != "") {  %>
          <div class="fila fila2">
            <label class="marca">MARCA</label>
            <p><%= data.producto[0].marca %></P>
          </div>
        <% } %>

        <% if(data.producto[0].modelo != "") {  %>
          <div class="fila fila2">
            <label class="marca">MODELO</label>
            <p><%= data.producto[0].modelo %></P>
          </div>
        <% } %>

        <% if(data.producto[0].agarre != "") {  %>
          <div class="fila fila2">
            <label class="marca">AGARRE</label>
            <p><%= data.producto[0].agarre %></P>
          </div>
        <% } %>

        <% if(data.producto[0].tipoDeVara != "") {  %>
          <div class="fila fila2">
            <label class="marca">TIPO DE VARA</label>
            <p><%= data.producto[0].tipoDeVara %></P>
          </div>
        <% } %>
          
        <% if(data.producto[0].tipoDeBolsa != "") {  %>
          <div class="fila fila2">
            <label class="marca">TIPO DE BOLSA</label>
            <p><%= data.producto[0].tipoDeBolsa %></P>
          </div>
        <% } %>

        <% if(data.producto[0].hierroTipoDeConjunto != "") {  %>
          <div class="fila fila2">
            <label class="marca"> CONJUNTO HIERROS</label>
            <p><%= data.producto[0].hierroTipoDeConjunto %></P>
          </div>
        <% } %>

        <% if(data.producto[0].color != "") {  %>
          <div class="fila fila2">
            <label class="marca"> COLOR</label>
            <p><%= data.producto[0].color %></P>
          </div>
        <% } %>
            
          
        </div>
      </div>
    </div>
  </div>


 
</main>
  
        <%- include('./partials/footer.ejs')  %>

</body>
</html>